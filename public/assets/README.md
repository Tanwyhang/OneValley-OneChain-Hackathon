Public assets directory

This folder mirrors the expected structure in GEMINI.md. Replace placeholders with the final art and audio assets.

Folder layout (create these if missing):
- tilesets/
- maps/
- sprites/
  - player/
  - npcs/
  - crops/
  - items/
  - effects/
  - ui/
  - meta/
- audio/

Add your Tiled JSON maps to `maps/` and sprite PNGs to the appropriate `sprites/` subfolders. Update `meta/manifest.json` when ready.
